# Implementation Plan: Obsidian Research Memory

**Spec**: `./spec.md` · **Date**: 2026-08-27

## Summary

`research_helper.vault`: `sync_vault` (reuses `research_helper.graph.build_graph`),
`write_current_context` (reuses `research_helper.tasks.load_active_task`).

## Constitution Check

§14 explicit requirement ("vault must be generated by script, not by the
LLM hand-writing links") is the design driver — `sync_vault` derives
every link from `build_graph`'s already-deterministic output. PASS.

## Project Structure

```text
research_helper/vault.py
tests/test_vault.py
```
